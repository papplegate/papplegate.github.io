[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pelican = {extras = ["markdown"], version = "*"}
ghp-import = "*"

[dev-packages]

[requires]
python_version = "3.12"

[scripts]
dev = "make devserver"
clean-grammar = "bash -c \"for file in Pelican-Cid/templates/*; do sed -i 's/>, and </> and the </g' $file; done\""
publish = "bash -c 'make html; ghp-import output -b gh-pages; git push git@github.com:papplegate/papplegate.github.io.git gh-pages:main'"
