[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pelican = {extras = ["markdown"], version = "*"}

[dev-packages]

[requires]
python_version = "3.11"

[scripts]
dev = "make devserver"
clean-grammar = "bash -c \"for file in Pelican-Cid/templates/*; do sed -i 's/>, and </> and the </g' $file; done\"" 
